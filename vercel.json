{
  "buildCommand": "cd painel && npm install && npx prisma generate --schema=./prisma/schema.prisma && npm run build",
  "outputDirectory": "painel/.next",
  "installCommand": "npm install && cd painel && npm install",
  "framework": "nextjs"
}
